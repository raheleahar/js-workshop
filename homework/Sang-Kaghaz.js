x=1+Math.floor(Math.random()*3);
y=1+Math.floor(Math.random()*3);
console.log ('1 is kaghaz---- 2 is gheychi ---- 3 is sang');
console.log('x: '+x);
console.log('y: '+y);
function SKG(x,y)
{
if(x>y)
{
console.log('The winner is x: '+x);
}
else
{
console.log('The winner is y: '+y);
}
}

SKG(x,y);
